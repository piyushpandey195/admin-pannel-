enum ButtonState { WAITING, ENABLED, DISABLED }

enum AppEnvironment { DEV, STAGING, PROD }

enum SellerRegistrationTab { AGETAB, CATEGORIESTAB, LINKTAB, CONTENTTAB }

enum InputFieldStatus { ENABLED, UNFILLED, CORRECT, ERROR }
